require './config/environment'        # Loads up environment
require 'sinatra/activerecord/rake'   # Rake tasks from sinatra-activerecord gem
                                      # Migrations can be created in Terminal with rake db:create_migration NAME=`migration_name`
                                      #   ex NAME: "create_dogs" to create dogs table with default 'change' method
